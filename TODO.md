# TODO

## Task: Fix Vercel `vercel.json` schema validation error (`additional property public`)
- [ ] Inspect current `vercel.json` contents
- [ ] Remove invalid `public` property from `vercel.json`
- [ ] Re-run Vercel schema validation locally (if possible) or ensure config matches Vercel spec
- [ ] Commit-ready change: ensure `vercel.json` remains valid JSON

